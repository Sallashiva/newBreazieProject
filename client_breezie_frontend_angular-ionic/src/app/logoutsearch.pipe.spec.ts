import { LogoutsearchPipe } from './logoutsearch.pipe';

describe('LogoutsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new LogoutsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
