import { SignatureDirective } from './signature.directive';

describe('SignatureDirective', () => {
  it('should create an instance', () => {
    const directive = new SignatureDirective();
    expect(directive).toBeTruthy();
  });
});
