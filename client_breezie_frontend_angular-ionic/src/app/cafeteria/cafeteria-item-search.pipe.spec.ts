import { CafeteriaItemSearchPipe } from './cafeteria-item-search.pipe';

describe('CafeteriaItemSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CafeteriaItemSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
