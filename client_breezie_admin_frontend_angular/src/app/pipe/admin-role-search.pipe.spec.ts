import { AdminRoleSearchPipe } from './admin-role-search.pipe';

describe('AdminRoleSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminRoleSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
